# Jetson AGX Xavier / JetPack 5.1.4 runtime overlay.
# Host Python 3.8 already provides the ABI-sensitive stack:
# torch/torchvision (NVIDIA CUDA 11.4 build), numpy, scipy, OpenCV and Pillow.
# DO NOT add/upgrade those here: env/setup_env.py pins the host versions.
transformers==4.44.2
tokenizers==0.19.1
safetensors==0.4.5
huggingface_hub==0.24.7
timm==1.0.9
einops==0.8.1
transforms3d==0.4.2

# MoGe-2 is cloned from a pinned source revision in `dependencies`, rather than
# installed as a package. Its package metadata requires Python >=3.9 even though
# the inference code used by this port is Python-3.8 compatible.
#
# MinkowskiEngine and pointnet2 are native CUDA extensions installed/built by
# run.sh for the host Torch 2.1 / CUDA 11.4 ABI.
